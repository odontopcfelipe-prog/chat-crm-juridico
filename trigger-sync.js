const axios = require('axios');

const API_URL = 'http://localhost:3005'; // Assuming the app is running locally or I can hit it
// Actually I need to hit the API with JWT

// Let's just use the SERVICE directly via node -e if possible
// Or just check if the app is even running
