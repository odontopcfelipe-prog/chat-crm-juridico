# Scripts shell SEMPRE com LF — rodam em Linux (VPS/containers via curl|bash).
# CRLF quebra o shebang ("bad interpreter: /bin/bash^M").
*.sh text eol=lf
entrypoint.sh text eol=lf

# Scripts Windows mantêm CRLF.
*.ps1 text eol=crlf
*.bat text eol=crlf
